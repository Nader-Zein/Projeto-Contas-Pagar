@model Pagamento.Models.Pais

@{
    ViewData["Title"] = "Editar País";
}

<h2>Editar País</h2>

<form asp-action="Editar" method="post">
    <input type="hidden" asp-for="IdPais" />

    <div class="d-flex justify-content-end mt-3">
        <div class="form-check">
            <input class="form-check-input" type="checkbox" asp-for="Status" />
            <label class="form-check-label" asp-for="Status">Ativo</label>
        </div>
    </div>
    <div class="col-md-1">
        <label asp-for="IdPais" class="form-label">ID</label>
        <input asp-for="IdPais" class="form-control" disabled />
    </div>
    <div class="col-md-3">
        <label asp-for="NomePais" class="form-label">Nome do País</label>
        <input asp-for="NomePais" class="form-control" maxlength="40" oninput="this.value = this.value.replace(/[^a-zA-Z\u00C0-\u00FF ]/g, '')" />
        <span asp-validation-for="NomePais" class="text-danger"></span>
    </div>

    <div class="col-md-2">
        <label class="form-label">Data de Criação</label>
        <input type="text" class="form-control" value="@Model.DataCriacao.ToString("dd/MM/yyyy HH:mm")" readonly />
    </div>

    <div class="col-md-2">
        <label class="form-label">Última Edição</label>
        <input type="text" class="form-control" value="@(Model.DataEdicao?.ToString("dd/MM/yyyy HH:mm") ?? "Nunca editado")" readonly />
    </div>

    <div class="text-end mt-3">
        <button type="submit" class="btn btn-success">Alterar</button>
        <a href="@Url.Action("Index", "Pais")" class="btn btn-secondary">Cancelar</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
