@model IEnumerable<Pagamento.Models.CondicaoPagamento>

<div class="modal fade" id="modalCondicaoCompra" tabindex="-1" aria-labelledby="modalCondicaoLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="modalCondicaoLabel">Selecionar Condição de Pagamento</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body">
                <input type="text" id="filtroCondicao" class="form-control mb-3" placeholder="Digite para buscar..." onkeyup="filtrarTabela('filtroCondicao', 'tabelaCondicoes')" />

                <table class="table table-bordered table-hover" id="tabelaCondicoes">
                    <thead>
                        <tr>
                            <th>Descrição</th>
                            <th>Ação</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            @foreach (var condicao in Model)
                            {
                                <tr>
                                    <td>@condicao.Descricao</td>
                                    <td>
                                        <button type="button" class="btn btn-sm btn-primary"
                                                onclick="selecionarCondicaoCompra(@condicao.IdCondPgto, '@condicao.Descricao')">
                                            Selecionar
                                        </button>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
                <hr />
                <button type="button" class="btn btn-success" onclick="abrirFormNovaCondicao()">
                    + Cadastrar Nova Condição
                </button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modalNovaCondicao" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content" id="modalNovaCondicaoContent">
        </div>
    </div>
</div>